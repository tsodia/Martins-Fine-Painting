# Google Sheets API (for lead pipeline)
GOOGLE_SHEETS_PRIVATE_KEY=
GOOGLE_SHEETS_CLIENT_EMAIL=
GOOGLE_SHEETS_SPREADSHEET_ID=

# Email Notifications (SMTP via SendGrid, Gmail, or other provider)
EMAIL_SERVICE_API_KEY=
EMAIL_FROM_ADDRESS=
EMAIL_TO_ADDRESSES=msodia@live.com,tyler.sodia@outlook.com
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey

# Google Analytics 4
GA_MEASUREMENT_ID=

# AI Instant Ballpark (photo-to-quote)
# Estimates always go to the internal lead email when the key is set.
# They are only SHOWN to the site visitor when ESTIMATE_SHOW_TO_CUSTOMER=true
# (flip only after Martin calibrates src/lib/pricing.ts).
ANTHROPIC_API_KEY=
ESTIMATE_SHOW_TO_CUSTOMER=false
ESTIMATE_MODEL=claude-sonnet-4-6
